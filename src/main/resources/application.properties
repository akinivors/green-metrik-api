# ===================================================================
# Environment Variable-Based Configuration
# ===================================================================
# The application will use the environment variable if it is set.
# Otherwise, it will fall back to the default value after the colon.

# Local PostgreSQL Settings
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/greenmetric_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Lale5146}

# JPA Settings
spring.jpa.hibernate.ddl-auto=create
spring.jpa.open-in-view=false
